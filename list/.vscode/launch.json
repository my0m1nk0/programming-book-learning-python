{
    "configurations": [
        

        {
            "type": "debugpy",
            "request": "launch",
            "name": "Launch Main",
            "program": "${workspaceFolder}/${input:programName}"
        }
    ],
    "inputs": [
        {
            "type": "pickString",
            "id": "programName",
            "description": "Select the Python file to launch",
            "options": [
                "list.py",
                "main.py",
                "node.py",
                "temp.py",
                "unorder_list.py"
            ]
        }
    ]
}